/**
 * src/config/api.js
 * Single source of truth for all base URLs.
 * Update here only — never hardcode URLs in screen files.
 */

/** Backend API (Node/Express on Render) */
export const API_BASE = 'https://streakboard.onrender.com';

/** Web frontend (Next.js on Vercel) — used for public profile share links */
export const WEB_BASE = 'https://streak-o.vercel.app';
